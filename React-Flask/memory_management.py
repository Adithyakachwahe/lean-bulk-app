# in python memory management is done using reference counting - when the reference counting is 0 then the memory is freed 
# for cycling reference we cannot do referece counting so we do garbage collector methods
